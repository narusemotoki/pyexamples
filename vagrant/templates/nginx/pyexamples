server {
	listen 80 default_server;
	index index.html index.htm;
	server_name localhost;
	location /transfer/bottle {
		include uwsgi_params;
		uwsgi_pass  unix:/var/run/uwsgi/transfer/bottle.sock;
	}
}